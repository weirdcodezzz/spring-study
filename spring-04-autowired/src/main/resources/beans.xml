<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        https://www.springframework.org/schema/context/spring-context.xsd">

    <context:annotation-config></context:annotation-config>

    <bean id="cat" class="com.luo.pojo.Cat"/>
    <bean id="dog" class="com.luo.pojo.Dog"/>

    <!--
        byName:会自动在容器上下文中寻找，和自己对象set方法后面值对应的 bean->id
        byType:会自动在容器上下文中寻找，和自己对象属性类型对应的 bean->id， 但是要保证全局id唯一
    -->
    <bean id="person" class="com.luo.pojo.Person">
    </bean>

</beans>